title: Testing
tagline: A brand new course on Learning Lab
description: Trying out the visual builder
template:
  repo: testing-template
  name: testing
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
